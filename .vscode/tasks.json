{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "sops: start auth server with local env",
      "type": "shell",
      "command": "npm run start:local",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "shared" }
    }
  ]
}
